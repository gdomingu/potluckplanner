<!--
  This is a snippet of HTML code that we reuse a lot. So we've extracted it
  into this "partial". Its local variables are set in the `locals` hash, which
  is defined when the partial is called (See form_helpers.rb).
-->

<input type="text" name="<%= name %>" value="<%= value %>">